{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/index.js",
  "scripts": {
    "start-electron": "set \"ELECTRON_ENV=DEV\" && tsc && electron .",
    "build-windows": " react-scripts build && set \"ELECTRON_ENV=PROD\" && electron-builder -c.publish.token=%GH_TOKEN% --publish always",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^5.27.1",
    "@typescript-eslint/parser": "^5.27.1",
    "electron": "19.0.4",
    "eslint": "8.17.0",
    "typescript": "4.7.3"
  },
  "dependencies": {
    "electron-updater": "5.0.1"
  }
}
